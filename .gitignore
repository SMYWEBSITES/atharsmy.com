# Dependencies
node_modules/

# Capacitor Android project (generated — run `npm install && npx cap sync android` to recreate)
android/

# iOS: tracked in git (contains custom Info.plist, AppIcon, Podfile.lock)
# Regenerate with: npm install && npx cap add ios && npx cap sync ios
# Then manually re-apply customisations from ios/App/App/Info.plist and Assets.xcassets

# Build outputs
dist/

# Environment / secrets
*.jks
*.keystore
local.properties
android/local.properties

# macOS
.DS_Store

# Editor
.vscode/
.idea/
